interface iProps {
  title: string, 
  type: string
}
export default function List({title, type}: iProps) {
  return (
    <div>
      <h1>{title}</h1>
      <div>
        
      </div>
    </div>
  )
}
